// TZK CTI - Malden Spawn Points (map-specific)
// Defines fixed starting positions for each side
// Called from init.sqf

// Default fixed spawn positions (overridden by custom start / MHQ deployment)
// FixStart_0 = WEST default (near La Riviere, south-west)
// FixStart_1 = EAST default (near Le Port, south-east)
TzkFixedStarts = [
	[3700, 3300, 0],
	[8200, 3100, 0]
];

diag_log "TZK CTI: Malden spawn points defined";
