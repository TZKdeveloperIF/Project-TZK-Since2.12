; args: [Command(string), <variable to be wait until(string, name of the variable)>]
? count _this < 2: goto "SkipWait"
@(call (_this select 1))
player globalChat (_this select 0)

#SkipWait
@!mutexCmd
mutexCmd = true
call (_this select 0)
mutexCmd = false
