; args: none

? not isServer: exit

_value = 0
_delay = blockTime / 6

# Update
	~_delay
	? (pvGameover != -1): exit

	_value exec "Net\hPingPpl.sqs"
	publicExec format [{%1 exec "Net\hPingPpl.sqs"}, _value call preprocessFile "Util\Num2Str.sqf"]

	_value = _value + 1
	goto "Update"