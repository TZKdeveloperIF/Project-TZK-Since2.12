? not isServer: exit

; args: [NetworkID[]]
_netIds = _this select 0; _this = 0
_c = count _netIds
? 0 == _c: exit

_i = 0
# loop
	[vehicle UnitById(_netIds select _i)] exec localize {TZK_BEHAVIOUR_LAND_HELI}

	_i = _i + 1
	? _i < _c: goto "loop"