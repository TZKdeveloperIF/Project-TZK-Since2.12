? isNull player: exit

; args: [x, y, w, h, dir, si, index]
? _this select 5 != siPlayer: exit

_marker = format ["ArtSvrArea%1", _this select 6]
_marker setMarkerPos [_this select 0, _this select 1]
_marker setMarkerSize [_this select 2, _this select 3]
_marker setMarkerDir (_this select 4)

_marker = format ["ArtSvrText%1", _this select 6]
_marker setMarkerPos [_this select 0, _this select 1]