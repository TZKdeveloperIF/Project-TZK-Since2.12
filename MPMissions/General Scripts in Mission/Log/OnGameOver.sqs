? not isNull player: if ("IF" != name player) then {exit}

? count TzkLog == 0: exit

_cfg = loadConfig "DebugLog.cpp"

_key = "TimeStamp"
{_key = _key + "_" + format ["%1", _x]} forEach missionStart

_cfg addValue [_key, TzkLog]

_cfg saveConfig "DebugLog.cpp"